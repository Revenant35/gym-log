import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zachbrown.GymProgressionApp',
  appName: 'gym-progression-app',
  webDir: 'dist/gym-progression-app/browser'
};

export default config;
